import { action } from '@storybook/addon-actions'
import { Stories, Story, Meta, Preview } from '@storybook/blocks'
// import { DesignTokenDocBlock } from 'storybook-design-token/dist/doc-blocks'

<Meta title="intro/style" />

# Style

You can define CSS variables here or in the "DesignTokens" panel under the component preview ("Canvas").

{/* <DesignTokenDocBlock categoryName="Style.css" viewType="table" /> */}
